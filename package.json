{
  "name": "m3-tags",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "homepage": ".",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/create-sites-worker.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name m3tags --branch main"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "typescript": "^5.7.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
